# CHANGELOG

このプロジェクトの問題生成ルール・パイプラインの変遷を、各バージョンで解決した課題ベースで記録する。バージョン番号は `lib/claude.ts` 内のコメント・コミットメッセージに基づく。日付はコミット日（JST基準の目安）。リーディングの生成ルール（`lib/claude.ts`）に関わらないアプリ全体の機能追加・UI再構成は「vX.X」ではなく見出しに直接内容を記す。

## v5.11 (2026-09-14)

**課題**: v5.10で読解解説（`ReadingQuestion.explanation`）の450字上限をハードエラー化しリトライに接続したが、実際に試験生成して検証したところ内容一致形式で実測500〜840字と大幅超過したまま配信されるケースが確認された。原因は「4択すべてに技法ラベル付きの理由説明を書かせる」という解説フォーマット自体が本質的に長く、1回のリトライでモデルが指示通りに縮められないことにあった（checkExplanationLengthの仕組みは正しく機能していたが、削るべき分量自体が大きすぎた）。ユーザーから「そこまで詳しい解説は不要」という方針転換の指示があり、フォーマットそのものを簡素化することにした。

- **読解explanationのフォーマットを根本から簡素化**（`lib/claude.ts`）: 新たに`READING_EXPLANATION_FORMAT_BLOCK`という共有プロンプトブロックを定義し、内容一致・空所補充の両フォーマットの`buildReadingOnlyStaticInstructions`で共通利用するようにした。新フォーマットは「【正解】1文のみ（本文の直接引用は禁止し、根拠の趣旨を要約する）」＋「4択すべての日本語訳（【1: 訳】〜【4: 訳】、choicesのA/B/C/Dに対応する1〜4の数字を使用）」の2要素のみで構成し、不正解選択肢の「なぜ誤りか」という理由説明を完全に廃止した。訳文の品質基準は語彙の【例文和訳】と同じ（直訳調・受動形の直訳禁止）。空所補充形式は従来「a brief Japanese explanation」としか指示していなかったため、このブロックの追加により初めて構造化された説明フォーマットを持つことになった
- **文字数上限を450字→300字に短縮**（プロンプト文言・`checkExplanationLength`呼び出しの両方、目安150〜250字）: `collectHardErrors`（読解全体の1回リトライ）と語彙側の`validateOneVocabQuestion`（該当なし、語彙は元々550字のまま変更なし）双方の読解向け呼び出し箇所（2箇所）を450→300に変更した
- **`verifyChoiceLabelConsistency`を読解の選択肢シャッフル経路から外した**: 同関数は解説中の`【N: word/snippet】`タグの中身と実際の選択肢の英文をfuzzy matchingで突き合わせる警告用チェックだが、読解のタグの中身が今回の変更で「英語の抜粋」から「日本語訳」に変わったため、英文と日本語訳を比較する既存ロジックは意味を失い、常に「記号ズレの疑い」を誤検知するだけになる。読解のシャッフル関数（`shuffleChoices`）内での呼び出しを、新設の`checkReadingExplanationChoiceCoverage`（4つの番号タグ【1】〜【4】が過不足なく1つずつ存在するかだけを見る軽量チェック、警告のみ）に置き換えた。語彙側（`shuffleChoicesWithTarget`）の`verifyChoiceLabelConsistency`はタグの中身が引き続き英単語のままなので変更していない
- **few-shot見本の更新**（`samples/fable5-v5/content-culture.fewshot.json`・`fillinblank-2.fewshot.json`）: 両ファイル計7問の読解`explanation`を、詳細な技法ラベル付き理由説明（250〜850字）から新フォーマット（103〜250字）に全面的に書き換えた
- **検証で見つかった残存リスク**: 試験生成中、1件（内容一致・4択中）で【1】【2】タグの訳文が実際の選択肢A/Bと入れ替わって出力される事例を確認した（プロンプトが「番号順に記述」と指示しているのみで、番号と選択肢内容の対応を明示的に自己検証させていなかったことが原因と考えられる）。プロンプトに「【1】〜【4】それぞれについて対応する選択肢の英文を指差し確認してから出力する」という出力前SELF-CHECK指示を追加したところ、その後の2回の独立した試験生成（計8選択肢×2=32件）では取り違えは再発しなかった。ただし`checkReadingExplanationChoiceCoverage`は番号の過不足しか検知せず、対応関係の正しさまでは機械的に保証できないため、既知の残存リスクとして記録する（旧フォーマットの`verifyChoiceLabelConsistency`も警告のみで自動修正はしていなかったため、保証水準としては同等）
- **検証**: ローカル`next dev`で内容一致・空所補充の両形式を複数回試験生成した。全ケースで解説が300字以内（実測120〜260字程度）に収まり、4択すべてに日本語訳が付き、不正解の理由説明が含まれていないことを確認した。番号↔選択肢の対応も、自己検証指示追加後の2回（計32選択肢）はすべて一致した

## v5.10 (2026-09-13)

**課題**: v5.9（解説への日本語訳追加）のリリース報告を受け、ユーザーから5点の品質改善依頼があった：(1) 読解choiceTranslationの訳文の直訳調、(2) 語彙で正解語が空所に文法的に入らない致命的バグ、(3) 読解解説450字上限が実質機能していない、(4) 正解語のタイポ（`sporadic`→`spooradic`）、(5) 解説中の誤答番号順の乱れ、(6) 簡体字混入チェックの誤検知。優先順位は(1)品質・(2)バグが必須、(3)〜(6)は同一セッションで対応可という指示だったため、全項目に対応した。

- **`ANNOTATION_MODEL`の既定値をHaiku 4.5からSonnet 5に変更**（`lib/claude.ts`）: 読解choiceTranslationで「大惨事的なシナリオ」「計測された開発」「認識された危険性にもかかわらず」のような英文の構造をなぞった直訳調が目立っていた。トークン単価は入力$2 vs $1、出力$10 vs $5（/1Mトークン、Sonnet 5がHaiku 4.5のちょうど2倍）。`generateVocabAnnotations`/`generateReadingAnnotations`に`logUsage`を新規追加して実測したところ、1日分のVocabAnnotations+ReadingAnnotationsの合計コストは概算$0.06→$0.12程度（約2倍）で、日次1回の生成のため絶対額への影響は軽微と判断した。Sonnet 5移行後の生成では、直訳調が明確に改善された自然な訳文を複数問確認した
- **語彙: 正解語（名詞）がコロケーション例の冠詞・動詞・前置詞を省略して裸で空所に入るバグを修正**（最優先・致命的バグ）: 正解語`charlatan`のコロケーション例`"be exposed as a charlatan"`に対し、生成された例文が`"...was eventually ____"`（`exposed as a`が脱落）となり、`charlatan`を代入すると非文になる事例を確認した。原因調査の結果、(a) プロンプトの名詞用法ガイド（`buildVocabStaticInstructions`）が`"a/the ____"`等の単純な枠しか示しておらず、コロケーション例自体が動詞・前置詞込みの複数語フレーズである場合の扱いを明記していなかった、(b) SELF-CHECK V1（「正解語を空所に入れた完全文を書き出し、文法的に成立するか確認する」）はプロンプト上の自己検証指示にすぎず、モデルが実際にこの検証を行った保証がないことが判明した。対応：①プロンプトの名詞用法ガイドとV1にコロケーション例の動詞・前置詞・冠詞を省略しないことをNG/OK例つきで明記、②`checkVocabBlankGrammar`（`lib/claude.ts`）を新規追加し、コロケーション例が冠詞付き名詞用法（`\b(a|an|the)\s+word\b`にマッチ）の場合のみ、生成された例文の空所直前が冠詞・限定詞で終わっているかを機械チェックする。誤検知回避のため、コロケーション例自体に冠詞がない語（不可算名詞等）は検査対象外とした。`validateOneVocabQuestion`のチェック⑨としてハードエラー化し、既存の単問リトライ（`retryVocabQuestion`）に接続した。ロジックを`charlatan`の実際のバグケースで単体検証し、検出できることを確認。不可算名詞ケース（`cause chaos`等）での誤検知がないことも確認した
- **読解解説の450字上限を実効化**（`checkExplanationLength`のハードエラー化）: 従来、語彙・読解ともにこの関数はStep 3.5で警告ログとして呼ばれるのみで、リトライ判定用の`collectHardErrors`/`validateOneVocabQuestion`には組み込まれていなかったため、実測713〜848字の超過が放置されていた（ユーザー報告時点では語彙側も同様に未実効だったが、報告文中の「語彙側の550字上限と同様に」という前提は誤りだったため、語彙側も合わせて修正した）。読解は`collectHardErrors`（両形式共通、`format==='content'`の分岐の外）に、語彙は`validateOneVocabQuestion`のチェック⑩として組み込み、それぞれ既存のリトライ機構（読解は1回のセット再生成、語彙は設問単位リトライ）に接続した。検証: 実測でリトライが実際に発火し、超過が713〜848字→467〜491字に縮小することを確認したが、1回のリトライで450字以内に完全収束するとは限らないケースも残った（v5.8で文書化した本文語数と同種の、LLMの分量制御能力の限界に由来すると考えられる。既知の制約として記録）
- **正解語のタイポ検知を追加**（`checkAnswerWordTypo`、語彙のみ、ハードエラー化）: 正解語`sporadic`が解説文中の自由記述箇所で`spooradic`（母音の二重化）と誤記される事例を確認した。まず`lib/wordbank.ts`側の静的データを疑い、Pythonスクリプトで全1300語超の`word`フィールドをmacOSの辞書ファイル（`/usr/share/dict/words`）と突合したが、誤字は検出されなかった（`sporadic`本体も正しいスペル）ため、これは生成時の一過性のLLM誤字であり、コードやデータの恒久的なバグではないと結論した。再発防止として、正解語と編集距離1以内かつ完全一致ではない綴りが解説文中に出現していないかを検査する軽量ヒューリスティック（`levenshteinAtMostOne`+`checkAnswerWordTypo`）を追加した。一般的な英単語が正解語とたまたま編集距離1になる誤検知（例: 正解語`retort`と無関係の英単語`report`、両者とも6文字で1文字置換のみの差）を避けるため、検査対象を「英単語の直後に空白なしで日本語の仮名（ひらがな/カタカナ/長音記号）が続く」箇所に限定した。この解説文体（`"deterrentが最適"`等）は英単語を日本語文中の用語として参照する場合にのみ現れ、`"annual report"`のような通常の英語プローズとしては現れないため、一般語との衝突リスクを実質的に排除できる。`validateOneVocabQuestion`のチェック⑪としてハードエラー化した
- **解説中の誤答参照順を番号昇順に整列**（`reorderExplanationChoiceSegments`）: プロンプトで「番号順（1→2→3→4）に記述すること」を指示しているが、モデルが生成順（バラバラ）のまま出力する事例を確認した。シャッフル時の`remapChoiceLetters`は番号の付け替えのみで文中の記述順は変えないため、この乱れは温存されたままだった。`【】`区切りのセグメントに分解し、`【N: word】`形式（誤答参照）のセグメントだけを番号昇順に並べ替え、`【正解】`/`【紛らわしいペア】`/`【例文和訳】`等の非数字セグメントは元の位置に残す関数を追加し、`shuffleChoices`/`shuffleChoicesWithTarget`内で`remapChoiceLetters`の直後・`verifyChoiceLabelConsistency`の直前に適用した
- **簡体字混入チェック（`SIMPLIFIED_ONLY_CHARS`、v5.2導入）の誤検知を修正**: 実際の生成物で「医学」「与える」「写真」「国」「当」「来る」に対し誤検知が発生していた。原因を調査した結果、日本の新字体（戦後の漢字簡略化）と中国大陸の簡体字が、別々の改革でありながら偶然同一の字形に収斂した文字（学/国/会/当/万/写/医/来/双/号/与の11字）がリストに混入しており、これらは「簡体字にしか存在しない字」という前提に反していたことが判明した。1字ずつ日本語の標準字体（新字体・旧字体とも）とUnicodeコードポイントを照合し、上記11字（うち`与`は重複登録もあった）をリストから除外した。完全な精度は前提とせず、頻出する明らかな誤検知のみを解消する方針とした
- **副次的な改善**: `generateVocabAnnotations`/`generateReadingAnnotations`が呼ばれた際にモデル名・トークン使用量を出力するよう`logUsage`を追加した（従来はレスポンス長のみをログ出力しており、モデル別のコスト実測ができなかった）
- **検証**: ローカル`next dev`から`/api/generate?refresh=true`・`/api/annotations`を複数回実行し、内容一致形式で確認した。語彙は全問が550字以内・末尾`【例文和訳】`・誤答番号昇順であること、読解explanationの450字超過時にリトライが発火し文字数が縮むこと（完全収束は保証されないことも確認）、Sonnet 5移行後の読解訳文の質的改善、簡体字誤検知の解消を確認した。`checkVocabBlankGrammar`は実際のバグ再現ケースに対する単体テストで検出を確認した
- **残る課題（今回のスコープ外）**: (1) 読解解説の450字上限は1回のリトライで必ず収束するとは限らない（語彙は複数回リトライ可能な設問単位のため収束しやすい一方、読解はセット単位の1回リトライのみ）。(2) 検証中に確認した既存の`verifyChoiceLabelConsistency`の誤検知（`【N: SCOPE_SHIFT】`のように`distractorType`ラベルをタグ内容として書いてしまい、実際の選択肢テキストと一致しないと警告される事例）はv5.5由来の別の既存課題であり、今回は対応していない

## v5.9 (2026-09-13)

**課題**: 語彙問題・読解問題の解説は日本語で書かれているが、解説が扱うのは「なぜその語・その選択肢が正解／不正解か」であり、**例文や選択肢そのものの日本語訳は解説に含まれていなかった**。学習時に英文の意味を自力で取り切れないまま解説を読むことになり、学習効果が落ちていた。調査の結果、解説の生成箇所は2系統（生成時プロンプト＝`GENERATION_MODEL`／アノテーション＝`ANNOTATION_MODEL`、いずれも既定は`claude-haiku-4-5`）に分かれており、**読解4択の和訳は既に`readingChoiceExplanations[].choiceTranslation`として生成・表示されていた**（`app/reading/page.tsx`・`app/quiz/[date]/page.tsx`の「訳：」）一方、**語彙の例文全体の和訳はどこにも存在しない**ことが判明した。そのため語彙はプロンプトへの新規追加、読解は既存プロンプトの補強という非対称な対応とした。

- **語彙: `explanation`末尾に`【例文和訳】`を追加**（`buildVocabStaticInstructions`）: 空所に正解語を入れた例文全体の自然な日本語訳を1文で出力させる。既存フォーマット（`【正解】`〜`【不正解】`〜`【紛らわしいペア】`の`【】`見出し方式・改行なしの単一文字列）を踏襲し、**追記位置はexplanation文字列の末尾に固定**した。直訳調の禁止、空所記号（`____`）を訳文に残さない、訳文中で選択肢番号を指す半角数字1〜4を単独で使わない（必要なら漢数字）を明記し、SELF-CHECKリストとJSON出力例にも反映した
- **語彙解説の文字数上限を400字→550字に緩和**（プロンプト文言と`checkExplanationLength`の両方）: 和訳ぶん（実測30〜60字）で従来の上限を恒常的に超過するため。実測値は383〜532字で新上限内に収まった
- **シャッフルとの干渉を防ぐコード側の安全網**（`lib/claude.ts`）: `remapChoiceLetters`は解説中の裸の数字1〜4（`([1-4])(?=は|が|と|を|の|に対応)`）も選択肢参照として書き換えるため、日本語訳文中の「3の」「1は」等を誤変換する恐れがあった。`VOCAB_TRANSLATION_MARKER`（`【例文和訳】`）で文字列を分割し、マーカーより前だけを再帰的にremapして後半をそのまま連結する実装に変更した。`verifyChoiceLabelConsistency`も同様に和訳セクションを照合対象外にし（和訳には`【N: 単語】`形式のタグが出現しないため）、無意味な「記号ズレの疑い」警告が出ないようにした
- **読解: `choiceTranslation`のプロンプト指示を補強**（`buildReadingAnnotationStaticRules`、内容一致・空所補充の両分岐）: データ構造・UI表示は既に4択すべての和訳に対応していたため変更せず、(1)「正解・不正解を問わず4択すべてに必ず出力（空文字・「同上」等での省略も不可）」、(2) 訳文の品質基準（受動形の直訳禁止・無生物主語の直訳回避・1文60字超は2文に分割・出力前に4択を読み返して直訳を書き直す）をNG/OK例つきで明文化した。`reading`（`ChoiceAnnotationSet`）側にも「A/B/C/Dの4キーすべてにtranslationを出力」を明記した
- **few-shot見本への反映**（`samples/fable5-v5/content-culture.fewshot.json`・`fillinblank-2.fewshot.json`）: 両見本の語彙5問ずつ計10問の`explanation`に`【例文和訳】`を追記した（JSONの整形は既存のまま維持し、差分は該当5行のみ）
- **表示側の変更なし**: `explanation`は`/reading`・`/quiz/[date]`・`/flashcards`で単一文字列としてそのまま表示され、UI側にパース処理は存在しないことを確認済み（PDF生成ルートは`/seidoku`・`/writing`のみで設問解説は出力していない）
- **検証**: ローカルの`next dev`から`/api/generate?refresh=true`と`/api/annotations`を実行し、内容一致（2026-09-13）と空所補充（`getTodayFormat`を一時的に固定して強制、検証後にrevert）の両形式を試験生成した。語彙10問すべてで`【例文和訳】`が末尾に生成され（例：「その財務責任者は、うっかり同社の機密買収戦略をある記者に明かしてしまい、交渉に大きな損害を与えたことに気づいた。」）、読解は両形式とも4択すべてに`choiceTranslation`が付いた。シャッフル後の`【N: 単語】`参照と実際の選択肢の対応も全問一致し、和訳セクションへのタグ混入・数字の誤変換は発生しなかった
- **残る課題（今回のスコープ外）**: (1) 読解の`choiceTranslation`は補強後も「認識された危険性にもかかわらず」のような直訳調が一部残る（`ANNOTATION_MODEL`がHaiku 4.5であることに起因する可能性が高く、さらに改善するなら同変数をSonnet 5に切り替えるのが有効）。(2) 今回の試験生成で、読解解説が450字上限に対し713〜848字と大幅超過していること、語彙例文で正解語が文法的に空所に収まらないケース（`was eventually ____`に名詞`charlatan`）、解説中の誤答が番号順に並んでいないケース、簡体字チェックの誤検知（「医学」「与」等）を確認した。いずれもv5.9の変更とは無関係な既存の品質課題であり、別途対応が必要

## v5.8 (2026-09-01)

**課題**: 直近6日分のレビューで、空所補充形式（3段落・3空所構成）は段落数こそ正しく3段落だったが、総語数が550〜700語程度あり、実際の英検1級本番Part 2（400〜450語、3段落）より明らかに長かった。調査したところ、原因は二重だった：(1) プロンプト側の語数指示（`buildReadingOnlyStaticInstructions`の`fillInBlankInstructions`）自体が「450-550語が許容範囲・500語が目標」となっており、本番より長い値を目標にしていた。(2) 機械チェック`checkPassageWordCount`（v5.2）は空所補充にも450〜550語のレンジを持っていたが「警告のみ・リトライには乗せない」実装だったため、範囲外でもログに警告が出るだけで実際の出力には何の効果もなかった。内容一致形式（500〜800語目安、既に良好と評価済み）のロジックには一切手を入れていない。

- **プロンプトの目標語数を修正**（`lib/claude.ts`の`fillInBlankInstructions`）: 「3-paragraph passage of 400-450 words total」「roughly 130-150 words per paragraph」「450 words is a HARD CEILING」に変更。SELF-CHECKリストにも「本文全体が400〜450語に収まっている」を追加した
- **`checkPassageWordCount`の空所補充判定レンジを380〜470語（バッファ込み）に変更**し、内容一致形式の550〜650語判定とは独立に扱うようにした
- **空所補充の本文語数をリトライ対象化**: 既存の読解リトライ機構（v5.5で導入、`generateQuestions`内の`collectHardErrors`）に`format === 'fill-in-blank'`のときのみ`checkPassageWordCount`のエラーを組み込んだ。これにより、既存の時間予算チェック（v5.6・150秒）、`afterTotal < beforeTotal`での採用判定、予算超過時の`throw`（キャッシュへのフォールバック）といった既存の仕組みをそのまま再利用でき、空所補充専用の新しいリトライ経路を作らずに済んだ。内容一致形式の`collectHardErrors`分岐（誤答精度チェック等）には変更なし
- **検証**: 本番モデル（`claude-sonnet-5`、`GENERATION_MODEL`環境変数）を使い、ダミー記事で空所補充を5回生成して本文語数をログ確認した（一時スクリプト、検証後に削除）。配信できた3件は411, 384, 433語で、以前の550〜700語から大幅に改善し、3/3（全体の3/5）が380〜470語のバッファ内（うち2/3が400〜450語の目標レンジ内）に収まった。3段落構成は配信できたケース全てで維持されていた。残り2件（367語・351語）は語数が範囲外だったが、並列実行の語彙生成が長引き時間予算（150秒）を超過したためリトライされずに`throw`され、配信されなかった（下記の既知の制約を参照）
- **既知の制約（未解決）**: (1) 語彙生成（並列実行）が長引くと読解の再生成に使える時間予算が残らず、語数超過のまま`throw`されキャッシュにフォールバックするケースがある（v5.6由来の既存挙動をそのまま踏襲した結果であり、今回のスコープ外）。(2) 1回のみのリトライでは語数が是正されない場合がある（LLMの語数カウント能力の限界による可能性）。再発時はリトライ回数の増加や段落ごとの語数指定強化を検討する

## v5.7 (2026-08-25)

**課題**: 本番の英検1級では長文読解の直前に英語タイトルが記載されているが、eiken-appの読解問題にはこれが存在しなかった。8/14に一度この機能を実装する計画があったが、実際にはコードが書かれておらず未実装のまま放置されていた（調査時、ページ上部のRSS記事出典表示（`data.article.source`/`data.article.title`、`app/quiz/[date]/page.tsx`等の`no-print`領域）と混同されていたことが判明）。今回改めて実装した。

- **読解生成プロンプトにタイトル生成指示を追加**（`lib/claude.ts`の`buildReadingOnlyStaticInstructions`、内容一致・空所補充の両フォーマット）: 本番EIKEN Grade 1スタイルの名詞句・短いフレーズ形式（例: "The Rise of Synthetic Realism"）、6語以内目安、本文の主題は示すが空所の答え・設問の結論を直接示唆しないことを明記。JSON出力例（`fillInBlankJsonExample`/`contentJsonExample`）にも`title`フィールドを追加した
- **`title`を既存の読解生成コール内で同時出力**（追加のLLM呼び出しなし）: `generateReadingOnly`の戻り値型・パース対象に`title: string`を追加し、`GeneratedQuestions`型（`lib/claude.ts`）にも`title`フィールドを追加した
- **few-shot見本の注記を修正**: `CONTENT_FEWSHOT_BLOCK`/`FILL_IN_BLANK_FEWSHOT_BLOCK`は「vocabQuestions/readingPassage/readingPassageJa/readingQuestionsの4フィールドのみが出力対象」という既存の注記があり、これをそのままにすると新設の`title`要求と矛盾してモデルが省略しかねなかったため、「titleはこの見本に含まれないが、別途メインの指示に従って必ず出力すること」を追記した
- **タイトルの機械チェックを新規追加し、既存のリトライ機構に接続**（`checkTitleValid`、`lib/claude.ts`）: 空文字でないこと、10語以内であることを検証する。従来「読解選択肢の語数・誤答精度チェック→1回だけ再生成」というリトライ機構（v5.5）は内容一致形式専用（`if (format === 'content')`）だったが、タイトルチェックは両形式に必要なため、リトライのトリガー条件（`collectHardErrors`/`overLengthCount`）を両形式で評価しつつ、内容一致専用のチェック（誤答精度等）は`format === 'content'`のときのみ実行するよう分離した。これにより空所補充形式でも、タイトル違反時に限り読解が1回再生成されるようになった（既存の35語超過・誤答精度チェックは従来通り内容一致形式のみ）
- **フロントエンドで本文直前にタイトルを表示**（`app/reading/page.tsx`、`app/quiz/[date]/page.tsx`）: 「第2部　長文読解問題」見出しの下・英文本文の上（問題表示・解答解説の両方、計4箇所）に`data.title`を表示。ページ上部のRSS記事出典表示（`text-sm text-gray-500`の小さいグレー文字）とは明確にスタイルを分け、中央揃え太字（`text-center text-lg font-bold`）で本番の見出しらしく見せている。専用のPDF生成ルートは読解問題には存在せず（`window.print()`でこの同じJSXがそのまま印刷される）、追加実装なしで問題用紙・解答解説PDFの両方に反映される
- **後方互換性**: `title`フィールドが存在しない旧キャッシュデータ（v5.7以前に生成された`.cache/*.json`）に対しては`{data.title && (...)}`で表示をスキップする

## v5.6 (2026-08-17)

**課題**: 本番で「問題の取得に失敗しました: SyntaxError: The string did not match the expected pattern」が発生（8/17、内容一致形式）。Vercelの本番ログ（`npx vercel logs`）で調査した結果、URL構築等のコード側バグではなく、`/api/generate`のサーバレス関数タイムアウトが原因と判明した：内容一致形式の読解生成が異常に長い出力（29,524トークン、262秒）を返した上、誤答精度チェック（v5.5）で検出したエラーに対して残り時間を考慮せず無条件に読解を再生成しようとし、`route.ts`の`maxDuration`（300秒）を超過。Vercelのプラットフォームタイムアウト応答は本文がJSONではないため、クライアントの`res.json()`が`SyntaxError`で失敗していた。

- **読解リトライに時間予算チェックを追加**（`lib/claude.ts`）: 読解生成開始（`genStart`）からの経過時間が`READING_RETRY_TIME_BUDGET_MS`（150秒）を超えている場合は再生成を諦める
- **時間予算超過時は下書きを配信せず生成を失敗させる（当初実装からの修正）**: 当初は時間予算超過時に警告ログのみで元の下書き（誤答精度チェック等でエラーが検出済みのもの。例: 正解選択肢が本文と5語以上連続一致）をそのまま採用していたが、これは「壊れた問題がそのまま配信される」バグだった（レビュー指摘で発覚）。`throw`してこの生成を失敗させ、`route.ts`の既存catchブロック（キャッシュ済みデータへのフォールバック、なければJSON形式のエラー応答）に処理を委ねるよう修正した
- **`/api/generate`にソフトタイムアウトを追加**（`app/api/generate/route.ts`）: `generateQuestions`を`withTimeout`（270秒）でラップし、`maxDuration`（300秒）のプラットフォームタイムアウトに達する前に必ずルート自身がJSONで応答できるようにした
- **注意（未解決）**: (1) 読解生成が29,524トークンもの出力になった根本原因（プロンプト側の要因の可能性）は本修正のスコープ外。再発時は本番ログの`output_tokens`と`stop_reason`を確認すること。(2) 時間予算とは無関係に、リトライを実施したがエラー総数が改善しなかった場合（`afterTotal >= beforeTotal`）も同様に元の下書きをそのまま採用する既存パス（v5.5から存在）があり、同種の「壊れた問題を配信し得る」リスクが残っている。今回のスコープは時間予算超過パスのみで、このパスは未修正

## アプリ機能追加: ライティング機能の追加 + UI再構成・リスニング機能削除 (2026-08-15)

**背景**: リーディング専用アプリだったeiken-appに、英検1級ライティング（大問4/5相当）対策として「週次エッセイ」「毎日の表現トレーニング」「隔週要約」の3機能を追加した。アプリが担当するのは各機能のお題生成とPDF化までで、手書き解答の添削・採点は範囲外（別スキル `writing-correction-review` またはChatGPT等での運用を想定）。あわせてトップ画面をリーディング/ライティングの選択式に再構成し、使用実績のなかったリスニング機能を削除した。

- **トップ画面の再構成**: `/` を新規のリーディング/ライティング選択画面にし、従来の `/`（今日の問題ページ）の内容はそのまま `/reading` に移設。`/history`・`/flashcards` 等から `/` への内部リンクは `/reading` に張り替えた
- **リスニング機能の削除**: `app/listening/`、`app/api/listening/`、`lib/listening{Data,Generate,Types}.ts`、`public/listening/*.mp3` を削除（git履歴に残るため復元は可能）。`app/api/tts/route.ts`・`components/SpeechPlayer.tsx` はリーディング側の例文・パッセージ読み上げでも使われているため維持
- **ライティング3機能を追加**（`/writing`、`/writing/essay`、`/writing/daily`、`/writing/summary`）: 生成は既存のリスニング機能と同じオンデマンド方式（ページを開いてボタンを押した時点で生成、cronは追加していない）
  - 週次エッセイ: 英検1級大問5相当。TOPIC文（"Agree or disagree: ~" / "Should ~?"形式）のみ生成し、賛否の論拠は提示しない。200〜240語（[英検公式2024年度リニューアルサイト](https://www.eiken.or.jp/eiken/2024renewal/)で確認済み）
  - 毎日の表現トレーニング: 論説文頻出構文（譲歩・因果・対比・言い換え）と1級語彙のストック36項目（`lib/writingExpressions.ts`）からコード側でローテーション選定し、LLMには短文作文指示のみ生成させる
  - 隔週要約: 3段落・300語程度のオリジナル英文パッセージを生成。要約は90〜110語（同公式サイトで確認済み）
  - 生成ロジックは新規 `lib/writingGenerate.ts`（`lib/listeningGenerate.ts`と同じ「独立サイブリングファイル」方針、`lib/claude.ts`には手を加えていない）。テーマ・表現の出題履歴による重複回避は `app/api/writing/_storage.ts` に実装し、`app/api/generate/route.ts`の`getRecentlyUsedWords`と同型のロジック（直近N件の履歴から除外Setを構成）を踏襲
- **PDF生成をpdfkitで共通化**: `app/api/pdf/seidoku/route.ts`にあった低レベルヘルパー（フォント登録・`hline`/`box`/`writeLine`・A4定数・バッファ収集）を`lib/pdfKit.ts`に抽出し、seidoku routeと新規`app/api/pdf/writing/route.ts`（`?type=essay|daily|summary&date=...`）の両方から利用する。3機能共通の「タイトルバー＋指示ブロック＋罫線ページ」レイアウトヘルパーを1つ実装し、機能ごとの差分（本文の有無・語数表示・罫線行数）のみパラメータ化した
- **pdfkit + Turbopackの互換性バグを2件修正**（新規PDF実装中に発見）:
  1. pdfkitは標準14フォントのAFMメトリクスを`fs.readFileSync(__dirname + '/data/...')`で実行時に読み込むが、Turbopackにバンドル・トレースされると`__dirname`が仮想パスに書き換えられ`ENOENT`になる。`next.config.ts`の`serverExternalPackages`に`pdfkit`を追加し、ネイティブ`require`のまま解決させることで回避した（`lib/pdfKit.ts`を使う`seidoku`route側にも効果があり、動作確認で初めて`/api/pdf/seidoku`のPDF生成成功を確認できた）
  2. `doc.heightOfString()`と実際の`doc.text()`で異なるオプション（`lineGap`）を渡すと、高さ計算が実際の描画より小さく見積もられ段落が重なって表示される。新規`app/api/pdf/writing/route.ts`ではオプションオブジェクトを共通化して両方に渡すよう実装した。同じパターンが`app/api/pdf/seidoku/route.ts`の既存コードにも残っているが、リーディング機能のロジック変更は今回のスコープ外のため未修正（要フォローアップ）

## v5.5 (2026-08-10)

**課題**: 読解（内容一致形式）の誤答が、本文と無関係な内容の創作・絶対表現の多用・不正解部分を長さで隠す等により見分けやすくなる/不自然になるケースがあった。誤答の質は既存のJapanese固定5ラベル（語句流用・内容ズレ/因果逆転/主語すり替え/極端化/本文に根拠なし）による指示のみでプロンプト側に委ねられており、機械的に検知・リトライできる項目が少なかった。正解選択肢のパラフレーズも語の言い換えに留まりがちだった。

- **`ReadingChoiceDraft`型を新規追加**（`lib/claude.ts`）: 読解の各選択肢に `text` / `isCorrect` に加え、`distractorType`（誤答のみ。`SCOPE_SHIFT`/`AGENT_SWAP`/`CAUSAL_REVERSAL`/`TIMELINE_SHIFT`/`MODALITY_SHIFT`/`HALF_TRUE_COMPOSITE`/`PURPOSE_RESULT_CONFUSION`の7種）、`sourceSpan`（根拠にした本文箇所。正解・誤答とも必須）、`falseElement`（誤答の誤っている最小部分）を持たせた。`ReadingQuestion`に`choiceDrafts?: ReadingChoiceDraft[]`として追加（追加のLLM呼び出しは発生させず、既存の読解生成呼び出し内で出力させる）
- **読解生成プロンプトを強化**（`buildReadingOnlyStaticInstructions`の内容一致形式）: 誤答は本文と無関係な内容を新規に創作しない、各誤答は本文情報を最低1つ正しく含み誤りは原則1箇所に限定する、長さで不正解部分を隠さず主体・範囲・因果・時制・確実性・目的と結果等の精密な差で不正解にする、絶対表現（always/never/every/entirely/completely/solely/regardless of等）は本文に同等の強さの主張がない限り使わない、3つの誤答は可能な限り異なる`distractorType`にする、という指示を追加。正解選択肢についても、本文と同じ語句の連続使用を避け主語・述語構造を変え上位概念へ言い換える（ただし本文から論理的に導けない抽象化はしない）指示を追加
- **誤答精度の機械チェックを新規追加し、既存のリトライ機構に接続**（`lib/claude.ts`。内容一致形式のみ）:
  - `checkWrongChoiceAbsoluteWords`: 誤答3つ中2つ以上に絶対表現を含む場合はエラー
  - `checkChoiceDraftSourceSpans`: 誤答の`sourceSpan`が欠落・空の場合はエラー
  - `checkDistractorTypeDiversity`: 誤答3つの`distractorType`がすべて同一の場合はエラー
  - `checkCorrectChoiceCopiesPassage`: 正解選択肢が本文と5-gram（連続5語）以上一致する場合はエラー（簡易文字列一致検査）
  - 上記いずれかでエラーが出た場合、既存の35語超過エスカレーション（`countOverMaxWordChoices >= 3`）と統合し、読解セット全体を1回だけ再生成する（`generateReadingOnly`にエラー内容を渡す既存の仕組みをそのまま再利用）。再生成後もエラー総数が改善しなければ元の生成結果を採用する
- **`choiceDrafts`は最終出力から除去**（`stripChoiceDrafts`）: 選択肢シャッフル後は生成順のメタデータが記号（A/B/C/D）と対応しなくなるため、バリデーション完了後・シャッフル前に取り除いてから保存する

## v5.4 (2026-08-02)

**課題**: 週次レビューで`GENRE_FEEDS`（`lib/rss.ts`）に3件のフィード重複が判明した。①火曜「サイエンス・テクノロジー」のBBC Scienceと木曜「環境・気候」のBBC Environmentが完全同一URL（`http://feeds.bbci.co.uk/news/science_and_environment/rss.xml`）、②火曜のScientific American 2件目・3件目がhttp/https違いのみの同一URL、③TIME（`https://time.com/feed/`）が土曜「文化・社会」と日曜「教育・テクノロジー」の両方で使われ、日曜のジャンル純度を下げていた（8/1にエネルギー地政学記事が混入した実例あり）。

- **候補フィードの疎通確認**: `debugFetchFeed`を使った一時スクリプトでNPR Science・Guardian Science・Ars Technica（`arstechnica/index`）・Ars Technica（`arstechnica/technology-lab`）の4件を確認し、いずれも記事取得OKと判明
- **「サイエンス・テクノロジー」（火曜）のフィード差し替え**（`lib/rss.ts`の`GENRE_FEEDS`）: BBC Science → NPR Science（木曜BBC Environmentとの重複解消）、Scientific American 3件目（https版） → Guardian Science（2件目とのhttp/https重複解消）
- **「教育・テクノロジー」（日曜）のフィード差し替え**: TIME → Ars Technica（`arstechnica/index`）。土曜「文化・社会」のTIMEはそのまま維持
- 環境・気候（木曜）・文化・社会（土曜）は変更なし

## v5.3 (2026-07-19)

**課題**: 読解パッセージの元記事取得（`lib/rss.ts`）が、曜日ごとのジャンル固有RSSフィード（各2件）→ BBC News/World固定フォールバックの2段階のみで、ジャンル固有フィードとBBC固定フィードの両方が同時に不調な場合、記事取得自体が失敗し生成が止まるリスクがあった。フィード数も各ジャンル2件と少なく、単一フィードの一時的な不調で早々にBBC固定へ落ちてジャンルの一貫性が崩れやすかった。

- **各ジャンルのフィードを2件→3件に増強**（`lib/rss.ts` の `GENRE_FEEDS`）: 月=Al Jazeera、火=Scientific American（https版を追加）、水=Guardian Business、金=NPR Health、土=Guardian Culture、日=Guardian Technologyを追加。木（環境・気候）は当初指示のGuardian Environmentが既存2件目と完全同一URLだったため、ユーザー判断でYale Environment 360に差し替え（Reutersは無料RSS廃止により実質死んでいる可能性が高いため除外）。実際の疎通確認で`https://e360.yale.edu/feed`は404で、正しいURLは`https://e360.yale.edu/feed.xml`と判明したため訂正
- **RSSパーサーにタイムアウトを追加**（`lib/rss.ts`、`new Parser({ timeout: 10000 })`）: Step Aで1ジャンルあたり最大3件のフィードを順に試すため、1件が応答なしで無期限にハングすると後続フィード・Step B/Cまで遅延するリスクがあった（デバッグ中に`npx tsx`のパッケージ初回インストールと紛らわしい長時間の待ちが発生し、タイムアウト未設定に気づいた）
- **記事取得を3段階フォールバックに再構成**: ①Step A（ジャンル固有フィード3件を順に試行）→ ②Step B（Step A全滅時のみ、AI生成記事にフォールバック）→ ③Step C（Step Bも失敗した場合のみ、BBC News/World固定フォールバック）。共通のフィード試行ロジックは`tryFeeds`に切り出し、Step AとCで共有
- **Step B: AI生成記事フォールバックを新規実装**（`generateArticleWithAI`、`lib/claude.ts`）: `GENERATION_MODEL`環境変数ではなく`claude-sonnet-5`に固定（低頻度経路のため品質優先）。`samples/fable5-v5/`の読解few-shotの文体を参考に550〜650語の記事を生成し、v5.2で導入したV1〜V3自己検証パターン（事実捏造チェック）を記事生成プロンプトにも適用した。`Article`型を介した`rss.ts`⇄`claude.ts`の相互参照は、`claude.ts`側を`import type`にすることで実行時循環importを回避
- **取得元のログ記録**: `console.log('[ArticleSource] step=A/B/C ...')`で毎回の取得元（Step A/B/Cのどれで、どのフィード/AI生成だったか）を記録。週次レビュー（`docs/weekly-review-checklist.md`）でStep B/Cの発生頻度を追跡できるようにした
- **テスト・デバッグ用のexport追加**: `tryGenreFeedsOnly(genreIndex)`（Step A単体実行）、`debugFetchFeed(url)`（フィード単体の疎通確認）。新規追加7フィードの疎通確認用に`scripts/check-feeds.ts`を新規作成

**申し送り事項**:
1. Step B（AI生成記事）はSonnet 5固定のため、Step Aが頻繁に落ちるようだと想定よりコストが嵩む。週次レビューで`[ArticleSource] step=B`の頻度を確認し、多い場合はフィードURLの健全性（リダイレクト・廃止等）を先に疑うこと

**追記（重大バグ修正、2026-07-19）**:
- **読解の選択肢/解説が本文と異なる記事の内容になる不整合を修正**（`app/api/annotations/route.ts`）: 本番で発見。`/api/generate`の本体生成（`questions:{date}`）は常に正しい記事を参照していたが、`/api/annotations`のキャッシュ（`annotations:{date}`）には対応する`questions:{date}`との紐付けが一切なく、同日中に`questions:{date}`が複数回上書きされた場合（`forceRefresh`、または未キャッシュ状態への同時アクセスによる競合生成）、古い記事に基づくアノテーションキャッシュがそのまま残存し得た。原因調査により、v5.3のRSSフォールバック変更自体が原因ではなく、以前から存在した`/api/annotations`側のキャッシュ鮮度検証の欠如が、当該日の複数回アクセスで顕在化したものと特定
- **修正**: `AnnotationData`に`questionsGeneratedAt`（対応する`questions.generatedAt`）を追加し、キャッシュ利用時に現在の`questions.generatedAt`と一致する場合のみ使うよう変更。`questions:{date}`が再生成されるたびに、次回の`/api/annotations`アクセスで自動的に鮮度不一致を検知し再生成されるため、`forceRefresh`時の明示的な`kv.del`に依存しない構造的な修正になっている
- ローカルで偽の古い`questionsGeneratedAt`を持つキャッシュを用意し、不一致検知→再生成が正しく動作することを確認済み

## v5.2 (2026-07-13)

**課題**: v5.1適用後、7/7〜7/12の6日間の生成物を評価した結果、語彙の出題済み語重複・正解位置の偏り・CEFR水準未達・固定コロケーションでの即答・読解の誤答技法の分類ブレ・簡体字混入等、複数の品質問題が判明した。

- **語彙の出題済み語を除外**（`app/api/generate/route.ts` の `getRecentlyUsedWords` / `lib/claude.ts` の `sampleWordBank`）: 直近30日分の生成済みデータ（KV `question_dates` / `.cache`）から語彙の正解語・誤答語を収集し、7/7〜7/12出題語のハードコードシード（`USED_WORDS_SEED`）と合わせて候補から除外する
- **CEFR B2以下語のブロックリスト**（`lib/wordbank.ts` の `CEFR_BELOW_C1_BLOCKLIST`）: collaborate/legitimate/extrovert/thermal/retrieve/extremist/eminent 等、C1未満と判明した語をsampleWordBankの候補から除外。週次レビューで追加判明した語を追記していく運用
- **語彙の正解位置を分散**（`shuffleChoicesWithTarget` / `assignBalancedTargetLetters`）: 5問を通じて同一の正解記号（A/B/C/D）が3回以上にならないよう、シャッフル前に正解位置を4種1巡ずつのランダム順で割り当てる方式に変更
- **警告のみの新規バリデーションを追加**（`lib/claude.ts`。いずれもリトライには乗せない）:
  - 本文語数チェック（内容一致550〜650語／空所補充450〜550語の範囲外で警告）
  - 中国語簡体字混入チェック（`SIMPLIFIED_ONLY_CHARS` ブロックリストによるベストエフォート検出）
  - 空所直前の冠詞(a/an)による選択肢の文法的排除チェック（語彙・穴埋め読解の両方）
  - 誤答分類ラベルのホワイトリスト照合（下記の新ラベル体系との一致確認）
- **語彙の誤答分類ラベルを2種に統一**: 旧「パターンA（意味が逆）/パターンB（焦点がズレる）/パターンC（文脈と無関係）」を「意味近接・焦点ズレ」「文脈と不整合」の2種に統合
- **読解（内容一致）の誤答技法を5種に再編**: 旧「技法1〜4＋本文に根拠なし」を「語句流用・内容ズレ（対比の言い換え・別段落混入を含む）／因果逆転／主語すり替え／極端化／本文に根拠なし」に再編し、同一設問内での型重複を禁止。本文と語彙的接点のない完全な捏造情報型誤答は使用を禁止
- **内容一致の4問が異なる段落を根拠とする**制約を追加
- **語彙の固定コロケーション即答（paradigm shift等）を禁止**し、文脈の論理（因果・対比・程度・方向）から正解を導く設計に変更
- **選択肢の参照を数字(1〜4)に統一**（`KEY_TO_NUM`/`NUM_TO_KEY`、`remapChoiceLetters`/`verifyChoiceLabelConsistency`）: UIが選択肢を1〜4の数字で表示するため、解説文中の参照もA/B/C/Dの記号から数字に変更（choicesオブジェクト自体のキーはA/B/C/Dのまま）。旧仕様のレター参照が残っていた場合の安全網は維持
- **読解本文の語数を実本番に近づける方向で調整**: 内容一致350〜450語→550〜650語、空所補充300〜400語→450〜550語（目標500語）。両形式とも「主張→根拠→反論・限定→総合」のアカデミックな論説構成を明示的に要求
- 見本データ（`samples/fable5-v5/*.json`）の解説ラベル文字列を新ラベル体系に一括置換
- `docs/weekly-review-checklist.md` を新規作成し、10項目の週次チェック表を整備

**追記（バグ修正、生成ルール自体の変更なし）**:
- **語彙生成のJSON解析失敗（"Failed to parse JSON from Claude response (vocab)"）に対応**: `parseJson`（`lib/claude.ts`）が```json ... ```/``` ... ```コードフェンスを検出してその中身を対象にしてから先頭`{`〜末尾`}`を抽出するよう堅牢化（従来は素のテキストに対する`{`〜`}`抽出のみ）。この関数は語彙・読解・アノテーション生成の全JSON解析経路で共通利用
- `buildVocabStaticInstructions`・`buildReadingOnlyStaticInstructions` のプロンプトに「JSONオブジェクトのみを返す（前置き・コードフェンス禁止）」の指示を追記
- 語彙・読解のJSON解析失敗時、従来はレスポンス先頭500字のみログ出力していたが、原因調査のため全文をログ出力するよう変更
- 語彙生成の`max_tokens`（12,000）は当初「確認の結果既に十分」として変更しなかったが、後日本番（Vercel）ログで`stop_reason: max_tokens, output_tokens: 12000`により出力が未完成のまま打ち切られ`No JSON object found`で失敗した実例を確認したため、16,000に引き上げ。通常のvocab生成は2,000〜2,400トークン程度で完了するため、これは暴走出力への安全マージンの拡大。あわせて語彙・読解の両方に`stop_reason === 'max_tokens'`の明示チェックを追加し、打ち切りが原因の失敗を「No JSON object found」等の紛らわしいメッセージではなくログ上で即座に切り分けられるようにした
- **実際にログで再現・特定した別の実行時エラー（"Cannot read properties of undefined (reading 'trim')"）に対応**: `choices`オブジェクトのキーがJSON構文としては正しいのに`"A"/"B"/"C"/"D"`ではなく`"1"/"2"/"3"/"4"`（数字文字列）で返ってくることがあった。解説文中の選択肢参照を数字(1〜4)にする上記v5.2ルールに引きずられ、`choices`自体のキーも数字にしてしまう挙動と見られる。値は4択とも正しいため`JSON.parse`は成功し、選択肢数チェック（4個か）も通過するが、`choices.D`等の参照がundefinedになり、選択肢シャッフル時の`verifyChoiceLabelConsistency`でクラッシュしていた。`normalizeVocabChoiceKeys`（`lib/claude.ts`、`generateVocabOnly`内でパース直後に適用）を追加し、数字キーで返ってきた場合は`answer`も含めてA/B/C/D表記に正規化する。プロンプト側にも「この数字ルールは解説文にのみ適用され、choicesオブジェクトのキーは必ずA/B/C/Dを使う」という明示的な注記を追記した
- **explanationフィールドの長さ上限をプロンプトに明記（暴走出力対策）**: max_tokens到達で打ち切られた事例（上記）は解説文が異常に長くなったことが一因と見て、`buildVocabStaticInstructions`に「【正解】1〜2文・各不正解1文・紛らわしいペア1文以内、全体400字以内」、`buildReadingOnlyStaticInstructions`（内容一致）に「【正解】2文以内・各誤答1文以内、全体450字以内」を明記し、両方のSELF-CHECKリストにも対応項目を追加。あわせて`checkExplanationLength`（警告のみ、リトライには乗せない）を追加し、上限超過が実際に発生していないか`generateQuestions`内でログ監視できるようにした

**申し送り事項**:
1. チェック項目「文法破綻ゼロ」「冠詞・前置詞による選択肢排除ゼロ」「出題済み語との重複ゼロ」が翌週ゼロにならない場合、生成と検証を別LLM呼び出しに分離する（検証はHaiku等の軽量モデルで可）
2. 内容一致の本文語数550〜650語は、本番（4問構成・約800語）より短い意図的な中間値である。本番完全再現に移行する場合は800語が最終目標

## v5.1 (2026-07-06)

**課題**: Sonnet 5への生成モデル切り替えに伴い顕在化した安定性の問題と、語彙問題の設問間重複・パイプラインのレイテンシ／タイムアウトリスクを解消する。

- WordBankを重複排除・品詞タグ付けして再構築し、語彙問題を「正解語1＋同品詞の誤答候補3」の固定4語グループとしてコード側で決定論的に抽選（`sampleWordBank`）。LLMには単語選定をさせず文だけを作らせることで、設問間の単語重複を構造的に排除（v5.1.1）
- 語彙生成・読解生成を独立したAPI呼び出しに分割し並列実行（vocabulary-first / Plan A）。語彙アノテーション・読解アノテーションも同様に呼び出しを分離し、バリデーション違反時は該当設問・該当アノテーションのみ再生成するよう変更（v5.1.3）
- 読解選択肢の35語上限について、プロンプト記述（旧: 25〜45語）と実際のバリデーション基準（35語）が不一致だったのを是正し、1セットで3件以上超過した場合のみ読解を1回再生成するエスカレーションを追加
- パッセージの自己言及表現（"the author contends" 等）を禁止する指示を追加
- 正解語のコロケーション例をプロンプトに明示し、非文的な語法を防ぐ安全網を追加
- Sonnet 5切り替えに伴い判明した読解生成の `max_tokens` 不足を是正（20,000→32,000）。読解アノテーションで `reading` フィールドが欠落するケースに対し、同一呼び出しで取得済みの `readingChoiceExplanations` から復元するフォールバックを追加
- `maxDuration` を300秒に延長（Vercel Fluid Compute前提）
- few-shot見本データ（`samples/fable5-v5/`）と回帰テスト記録（`samples/regression-v5_1/`）をリポジトリに追加

## v4 (2026-06-15 〜 2026-06-21)

**課題**: 読解選択肢の解説粒度が粗く、なぜ誤答なのかの技法的な説明が不足していた。また旧パイプラインの校閲・難易度評価ステップが原因でAPIタイムアウト（60秒）が頻発していた。

- 読解選択肢ごとに構造化された解説（`readingChoiceExplanations`）を追加。誤答技法（技法A「方向性の逆転」/技法B「部分的整合」等）をラベル付きで明示
- アノテーション生成を `/api/annotations` として問題生成本体から分離し、問題表示後に非同期で解説を取得できるようにした
- 生成→校閲→難易度評価→（必要時）再生成という多段パイプラインを廃止し、ステップ数を削減してタイムアウトを解消
- 形式判定（内容一致 / 空所補充）をUTC基準からJST日付基準に修正（深夜帯に連日同じ形式になるバグの修正）
- 語彙アノテーションのデータ混入バグ（他の設問の解説が紛れ込む）を修正

## v3 (2026-06-15)

**課題**: 精読ノート・解説の質が不十分で、学習者が誤答の理由を自力で理解しにくかった。

- 精読ノート・解説を全面改訂（構文解析・パラフレーズ対応・語彙注釈の拡充）
- アノテーション生成をキーベースのマッチング方式に変更し、`max_tokens` を増加。プロンプトを軽量化

## v2 (2026-06-06 〜 2026-06-14)

**課題**: 初期バージョンの問題は実際の英検1級過去問と比べて選択肢の作り込みが甘く、語彙問題の質を担保する仕組みもなかった。

- 問題生成ルールを全面改訂。読解誤答選択肢の作成ルールを精密化し、技法ベースで誤答を設計する方針を導入
- 語彙問題に5項目の自己チェックをプロンプトに追加
- 生成→自動校閲の2段階方式、および難易度評価AIによる生成→校閲→評価→（必要時）再生成のフローを導入（後にv4でタイムアウト対策のため撤去）
- 選択肢の正解位置をランダムシャッフルする仕組みを追加
- 解説・精読ノートを改訂し、選択肢解剖（ChoiceDissect）をメインページにも追加
- 精読ノートのPDF出力をHTMLページ方式に変更（Vercelのサーバーレス環境でPDF直接生成すると白紙になる問題への対応）

## v1 (2026-04-23 〜 2026-06-06)

**課題**: 英検1級レベルの語彙・読解学習を毎日自動化したい、という最小要件から出発。

- Create Next Appからプロジェクトを開始し、Vercel KV（Upstash）接続、ニュース記事RSS取得、語彙穴埋め＋読解問題の生成基盤を構築
- 日替わりジャンルローテーション、記事本文抽出のフォールバック処理、JSON解析の堅牢化など基礎的な安定化を実施
- 過去30日分の問題履歴、翌日語彙復習用フラッシュカード、Google TTSによる読み上げ機能を追加
- 英検1級単熟語（約2,230語、後のバージョンで重複排除・品詞タグ付けして再構築）を単語帳として統合し、語彙問題に活用開始
- 実際の英検1級過去問（2024年）に基づきプロンプトを全面刷新
- リスニング過去問ページ、日次リスニング問題自動生成（フェーズ2）、精読ノートPDF自動生成を追加
